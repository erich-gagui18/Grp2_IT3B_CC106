<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Beteranos">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ReservationActivity"
            android:exported="false" />

        <activity
            android:name=".AdminLoginActivity"
            android:exported="false" />

        <activity
            android:name=".AdminDashboardActivity"
            android:exported="false" />

        <activity android:name=".ui_reservation.reservation.services.HaircutActivity"/>
        <activity android:name=".ui_reservation.reservation.services.HairColorActivity"/>
        <activity android:name=".ui_reservation.reservation.services.HairwashActivity"/>
        <activity android:name=".ui_reservation.reservation.services.BarbersActivity"/>
        <activity android:name=".ui_reservation.reservation.services.PromoActivity"/>
        <activity android:name=".ui_reservation.reservation.services.ScheduleActivity"/>

    </application>

</manifest>